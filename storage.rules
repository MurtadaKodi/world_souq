rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read: if true;   // ✅ السماح للجميع بالقراءة
      allow write: if request.auth != null;
    }
  }
}
